export APP_ROOT=$(pwd)
export PATH=$APP_ROOT/bin:$PATH

export DOCKER_WRAPPER_IMAGE_node=10.11.0-stretch
export DOCKER_WRAPPER_SERVER_OPTS_npmserver="-p ${LABO_PORT_PREFIX}80:8080"
export DOCKER_WRAPPER_APP_TARGET=npmserver
